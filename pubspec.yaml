name: instagram_clone_assignment
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  cached_network_image: ^3.4.1
  flutter:
    sdk: flutter
  font_awesome_flutter: ^11.0.0
  insta_image_viewer: ^1.0.4
  photo_view: ^0.15.0
  provider: ^6.1.5+1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: Billabong
      fonts:
        - asset: assets\fonts\Billabong.ttf.ttf
